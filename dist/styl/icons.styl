@font-face {
  font-family: "stylicons";
  src: url('../fonts/stylicons/stylicons.eot');
  src: url('../fonts/stylicons/stylicons.eot?#iefix') format('eot'),
          url('../fonts/stylicons/stylicons.woff') format('woff'),
          url('../fonts/stylicons/stylicons.ttf') format('truetype');
}

icon-base() {
  font: normal normal 1em/1 stylicons
  text-rendering: auto
  display: inline-block
  -webkit-font-smoothing: antialiased
  -moz-osx-font-smoothing: grayscale
  font-style: normal
  font-variant: normal
  font-weight: normal
  vertical-align: sub
  // speak: none; // only necessary if not using the private unicode range (firstGlyph option)
}

icon-char($filename) {
  $char = ""
  
  if $filename == heart {
    $char = "\E001"
  }
  
  return $char
}

make-icon($fileName, $insert = 'before', $extend = false) {
  &:{$insert} {
    if $extend {
      icon-base()
    }
    content: icon-char($fileName)
  }
}

.#{$icon-class} {
  icon-base()

  &.heart {
    make-icon(heart)
  }

}